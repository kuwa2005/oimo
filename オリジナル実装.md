# Open Mimo Code オリジナル実装

このドキュメントは、Open Mimo Code (`oimo`) に実装されている機能のうち、Upstream の **opencode** (sst/opencode) にもフォーク元の **Xiaomi MiMo Code** にも存在しない「オリジナル実装」をまとめたものです。

フォーク元 (MiMo Code) の最新コミットの上に追加された kuwa2005 のコミット群を根拠としており、各項目に該当コミット・実装ファイルを記載しています。

## 系譜（3 段フォークの全体像）

`oimo` は単一のフォーク元から派生したのではなく、**2 つの祖先から段階的にフォークされた**製品です。

```
opencode（anomalyco/opencode）       [既存リポジトリ: `original` リモート]
   └─フォーク→ Xiaomi MiMo Code      [既存リポジトリ: `upstream` リモート]
         └─フォーク→ Open Mimo Code  [既存リポジトリ: `origin` リモート]
```

- `git remote` の構成根拠:
  - `original` → `https://github.com/anomalyco/opencode.git`
  - `upstream` → `https://github.com/XiaomiMiMo/MiMo-Code.git`（push は禁止・pull-only）
  - `origin` → `https://github.com/kuwa2005/oimo.git`
- つまり **OpenCode のコア機能は、MiMo Code を経由して oimo へ受け継がれている**。両フォーク元から「何を引き継ぎ、何をさらに進化させたか」を次節で整理する。

---

## OpenCode から進化した点

OpenCode 由来のコア基盤を**保持・強化**しつつ、oimo 独自に進化させた点。

### 保持した OpenCode コア機能（そのまま継承）
- 複数 LLM プロバイダー接続、ターミナルネイティブ TUI、LSP 連携、MCP サーバー、プラグイン機構
  → README「OpenCode との関係」に明記（`OpenCode の全コア機能（複数プロバイダー、TUI、LSP、MCP、プラグイン）を保持しつつ`）

### OpenCode から受け継ぎ oimo で進化させた点
| 受け継いだ機能 | oimo での進化 | 実装/設定 |
|---|---|---|
| 単一セッションの会話 | **持続的メモリ**（SQLite FTS5 全文検索）・セッションチェックポイント・タスク進捗の自動保存・再開時注入 | `MEMORY.md` / `checkpoint.md` / `notes.md` / `tasks/<id>/progress.md` |
| 固定コンテキスト窓の圧縮 | **インテリジェントコンテキスト管理**（自動チェックポイント・再構築・予算制御インジェクション・`/context-limit` によるモデル別圧縮点調整） | `compaction.max_context` |
| 単一プライマリエージェント | **マルチエージェントオーケストレーション**（build/plan/compose、オンデマンドサブエージェント、並列・キャンセル・バックグラウンド実行） | `Tab` で切替 |
| 「やった」という楽観的停止 | **ゴール駆動の自律ループ**（`/goal` 停止条件を独立ジャッジモデルが判定） | `src/session/goal.ts` |
| 単発実行 | **compose ワークフロー／組み込みスキル**（仕様駆動・並列・非対話の決定論的パイプライン） | `.oimo/workflows/` |
| チェックリスト型の案件遂行 | **dream / distill / evolve による自己改善ループ**（経験の結晶化・Skill 化・AI-to-AI 改造指示書） | `evolve` Skill ほか |

### oimo が OpenCode 互換のために追加したもの
- OpenCode Zen（Big Pickle 等）との通信互換ヘッダー（§11）: 公式クライアント識別ヘッダーを付与
- `run --auto` の移植（付録）: `--yolo` / `--dangerously-skip-permissions` エイリアス

---

## Mimo Code から進化した点

Xiaomi MiMo Code から受け継ぎ、oimo でさらに進化させた点。

### 保持した Mimo Code 機能（そのまま継承）
- MiMo エコシステム連携（Xiaomi MiMo モデル）、音声入力（TenVAD + MiMo ASR）
- `mimo` プロバイダー / `mimo-auto` モデル（term: 中国国内チャネル）

### Mimo Code から受け継ぎ oimo で進化させた点
| Mimo Code 由来 | oimo での進化（対応節） |
|---|---|
| 無料モデルチャネル（mimo-auto） | 終了を受けて **`auto/free`（OpenCode Zen 無料枠）へ置換**・first-frame のみフォールバック（§3）。中国国内サーバ送信を防止する **`auto/free` 非フォールバック**設計 |
| デフォルト有効のアナリティクス送信 | **opt-in 化**（`MIMOCODE_ENABLE_ANALYSIS` のみ送信）（§4） |
| mimo-free チャネル | **明示オプトイン化**（`MIMOCODE_ENABLE_MIMO_FREE`）。Tor 併用時のフィンガープリント回転（§5） |
| 定型フィードバックループ | **SE / FDE / Super Auto 自律実行**（`--se` / `--fde` / `--spauto`）、**Friction Learning + Character**（§1e）、ヒアリングまたは現場課題定義→ロック→非停止実装→独立ジャッジ判定（§1） |
| 標準エージェント動作 | **compose エージェント／仕様駆動開発**をさらに拡張（ワークフロー・スキル・評価ゲート） |
| MiMo Code のブランド | **Open Mimo Code へリネーム**、`oimo` バイナリ・`.oimo` 設定・`oimo.jsonc`・テーマ（bright purple）（§8） |

> 注: Mimo Code から「受け継ぎ・進化」したものと、どちらのフォーク元にもない「完全オリジナル実装」（→ 次節 §1〜§12。§1 には SE / Super Auto / FDE / Friction Learning / Anti-burn、§12 にはマルチリポジトリを含む）は区別して扱う。

## 1. SE 型自律実行モード (`oimo --autonomy` / `oimo --se`)

ソフトウェアエンジニア (SE) 型の AI 自律実行モード。要件をヒアリングしてロックしてから、非停止で実装を続け、独立したジャッジが「書類証跡」をもとに完了を判定するまで進行します。

- 起動フラグ: `oimo --autonomy` / `oimo --se`（エイリアス）、環境変数 `MIMOCODE_AUTONOMY`
- 挙動:
  - `compose` エージェントを暗黙指定（`src/cli/cmd/tui/thread.ts`）
  - `hearing_first`（既定 true）の間は要件ヒアリング・質問フェーズ → 要件ロック後 (`goal.phase === "execute"`) に `never-ask` と `skipPermissions` を自動有効化（`src/cli/cmd/tui/app.tsx`）
  - ロックゲートヘッダ: **Requirements Lock**（日英別名あり）。`persona` 未設定時は SE
  - 停止条件にはドキュメント証跡（ヒアリングログ、要件/仕様・テスト基準、検証結果）が必須
- 設定キー（`opencode.json` / `oimo.jsonc` の `autonomy`）: `enabled` / `hearing_first` / `persona`（`se` | `fde`）/ `docs_evidence` / `max_turns`（既定 50）/ `max_duration_ms`（既定 2 時間）/ `max_cost_usd`（既定 10）/ `judge_max_retries`（既定 2）
- 実装: `src/config/autonomy.ts`、`src/cli/cmd/tui/thread.ts`、`src/cli/cmd/tui/app.tsx`
- コミット: `1cd4d9038` (feat: SE-style autonomy mode and restore mimo-auto free channel)、`6967010fc` (fix(autonomy): make test spec and executed tests mandatory stop-condition items)

### 1b. Super Auto (`oimo --spauto` / `oimo --autosp`)

SE 自律と同じジャッジ・証跡スタックだが、**最初のヒアリングすらユーザに聞かず**、曖昧点はすべて自律判断して完全ノンストップで遂行するモード。

- 正式名: **Super Auto**。CLI 正式フラグ: `--spauto`（エイリアス `--autosp` も可）
- 環境変数: `MIMOCODE_SPAUTO`（互換で `MIMOCODE_AUTOSP` も可）
- 挙動:
  - **起動のたびに**赤ベースの警告画面でリスク承認を要求（拒否が既定。対話 TTY 必須・永続スキップなし）
  - 承認後のみ `autonomy.enabled=true` かつ `hearing_first=false` → goal は最初から `execute`
  - 承認後は `never-ask` + skip-permissions + ワークスペース信頼スキップ + `MIMOCODE_AUTO_APPROVE_DELETE` により、TUI 上で承認・入力待ちに止まらない
  - 自己回答した仮定をヒアリングログに残し、要件/テスト証跡は通常の autonomy と同じく必須
- 実装: 上記 autonomy パス + `Flag.MIMOCODE_SPAUTO` + `promptSuperAutoWarning`（`thread.ts`）

### 1c. Forward Deployed Engineer モード (`oimo --fde`)

SE（慎重な要件ロック）とは**別ペルソナ**の現場エンジニアモード。問題定義→ Level 1–3 提案→ **PoC 許可**→ **Solution Lock**→ 実装・検証・改善提案まで。

- 起動フラグ: `oimo --fde`、環境変数 `MIMOCODE_FDE`（+ `MIMOCODE_AUTONOMY`）
- `autonomy.persona = "fde"`、`hearing_first = true`
- 挙動:
  - compose エージェント。hearing 中は PoC/スパイク可、本番機能・大規模構成は Solution Lock 後
  - ロックゲートヘッダ: **Solution Lock**（`解決策ロック` 等の日英別名）。`question` ツールで承認すると `phase=execute`
  - 停止証跡: Understanding / Problem / Solutions(L1–3) / PoC / Implementation / Verification（可能なら Before/After）/ Next Improvement ≤3
  - プロンプト: `src/session/prompt/autonomy-fde.txt`
- **併用:** `--se --fde` 可。自律ペルソナは FDE（Solution Lock）、**Friction Learning** は SE/FDE 双方レンズで分析（§1e）
- **引き継ぎ:** `/auto` で mid-session 切替（none / normal / fde / special、global config に永続）、または `oimo -c --fde` / `oimo -c --se`。**`-c` のみでは CLI フラグは再適用されない**（プロセス env のみ）
- TUI: `/auto` ダイアログ、`POST /config/autonomy-mode` が `"fde"` を受付
- 実装: `src/config/autonomy.ts`、`src/flag/flag.ts`、`thread.ts` / `session.ts`、`prompt.ts`、`tool/question.ts`、`dialog-auto-mode.tsx`
- コミット: `cffd7815b1` (feat: add --fde Forward Deployed Engineer autonomy mode)

### 1e. Friction Learning + Character（`--se` / `--fde` 統合）

ユーザーの追加指示・やり直し・否定・後出し条件を、単なる修正要求ではなく **期待と結果の摩擦 (Friction)** として検出し、次回タスクへ反映する。専用 CLI（`--friction-learning` 等）は設けない。

- **有効条件:** `--se` および/または `--fde`（環境変数 `MIMOCODE_FRICTION_SE` / `MIMOCODE_FRICTION_FDE`）
- **SE レンズ:** 実装・検証・非機能・完了条件の不足（Instruction / Verification Gap 等）
- **FDE レンズ:** 業務目的・利用者・現場運用・本当の期待（Requirement Discovery / business_intent）
- **フロー:** Detect → Classify → Root cause → Rule candidate → Scope/Confidence → Memory → 次回自動補完
- **保存:** `~/.oimo/evolve/<projectID>/friction/fl-*.md` + `learning-rules.json`、プロジェクトは `.oimo/friction/rules.json`
- **Scope:** Task / Session / Project / User / General（1回で General trusted へは昇格しない。`今回だけ` は Task）
- **誤学習修正:** 「今後その確認はいらない」等で disable / confidence 低下
- **evolve:** 繰り返し観測されたルールのみ evolve 候補（`friction/evolve-bridge.ts`）。毎回コード書き換えしない
- **dream:** `dream.txt` Phase 2 で friction イベントと rules を統合対象（類似ルール統合）
- **Character（提示のみ）:** `--character=default|off`（未指定=default）。推論・責任分類・学習内容は変更しない
  - `default`: 率直・軽く愚痴可・必ず改善案
  - `off`: 中立表現。学習・Memory・次回適用は継続
  - 将来: `osaka` / `tsundere` 等を `CharacterMode` 追加 + renderer のみで拡張可能
- **実装:** `src/friction/**`、`src/character/**`、`session/prompt.ts` 注入、`cli/cmd/tui/thread.ts`
- **テスト:** `test/friction/learning.test.ts`（Scenario A–G、次回適用、Character 非干渉）

### 1d. 自律ループ Anti-burn（API 食い潰し防止）

モデルが「完了・次の指示待ち」と宣言しても rate-limit 復旧や goal 再突入で延々と回り続ける問題への対策。

- **完了/待機検知:** アシスタント本文が「次の指示をお待ち」「全作業完了」「all tasks complete」等なら goal を停止  
  - hearing → `stopReason=waiting_user`  
  - execute → `stopReason=completed`  
  - 実装: `isAwaitingUserOrDone` / `assistantVisibleText`（`src/session/prompt/text-loop-recovery.ts`）、`goalGate`（`prompt.ts`）
- **rate-limit 自動再試行:** 完了/待機なら `skip_done`（再キックしない）。同一 storm の上限を **2 回**に削減（`RATE_LIMIT_AUTO_RETRY_MAX`）
- **hearing 中 think-only 自動継続:** 上限 **2**（`INVALID_OUTPUT_HEARING_LIMIT`）。execute / Super Auto は従来どおり最大 8
- **ユーザ入力なし goal re-entry:** 連続 **5 回**で `waiting_user` 停止（`GOAL_REENTRY_NO_USER_HARD`）。ツール進捗または実ユーザ発話でリセット
- ドキュメント: `mimocode-docs/reference/config.md`（Anti-burn）

---

## 2. Tor SOCKS5 プロキシ経由の通信ルーティング (`oimo --tor`)

oimo 自身のネットワーク通信（モデル API 呼び出しなど）をすべて Tor SOCKS5 プロキシ経由でルーティングします。

- 起動フラグ: `oimo --tor`、環境変数 `MIMOCODE_TOR`（有効化）/ `MIMOCODE_TOR_PROXY`（プロキシ指定）
- 挙動:
  - ローカル Tor デーモン必須。プロキシ既定値は `socks5h://127.0.0.1:9050`
  - SOCKS5 no-auth ハンドシェイク + CONNECT を独自実装（`src/util/tor.ts`）
  - ホスト名は ATYP 0x03 でプロキシ側に送り**リモート DNS 解決**を行い、ローカル DNS リークを防止
  - TUI ワーカーにも伝播（`src/cli/cmd/tui/worker.ts`）
- 実装: `src/util/tor.ts`（237 行）、`src/index.ts` のミドルウェア (`initTor`)、`src/flag/flag.ts`
- テスト: `test/util/tor.test.ts`（136 行）、`test/util/tor.integration.test.ts`
- コミット: `a6a75bb06` (feat: route network traffic through Tor (--tor))

---

## 3. 未設定時のデフォルトモデル = Auto (無料) (`auto/free`)

プロバイダ・モデル未設定のクリーンインストール時に、デフォルトで仮想モデル `auto/free` を選択します。実行時は FCC 同期カタログに基づく無料候補を **first-frame 前のみ** フォールバックします（先頭は通常 `opencode/big-pickle`）。Xiaomi の `mimo-auto` チャネルにはフォールバックしません。

- 挙動:
  - 公開 ID `auto/free` をメッセージに保持し、上流は認証済み無料候補を順に試行
  - サーバ側 `Provider.defaultModel` と TUI `local.tsx` が `auto/free` を優先
  - 純正 mimo モード (`MIMOCODE_EXPERIMENTAL` / `MIMOCODE_MIMO_ONLY`) 時のみ従来どおり `mimo-auto` へフォールバックしうる
- 実装: `src/provider/auto-free/`、`src/session/processor.ts`、`src/provider/provider.ts`、TUI `dialog-model.tsx`
- カタログ同期: `bun script/sync-auto-free-catalog.ts`、`docs/auto-free-fcc-sync.md`
- 将来の `auto/paid` / `auto/hybrid`（無料→有料エスカレーション）: `docs/auto-mode-roadmap.md`

---

## 4. アナリティクス計測の opt-in 化 (`MIMOCODE_ENABLE_ANALYSIS`)

`tracking.miui.com` へのメトリクス送信（`model_call` / `tool_call` / `agent_request`）をデフォルトで**無効化**しました。明示的に `MIMOCODE_ENABLE_ANALYSIS=true` を設定した場合のみ送信されます（フォーク元ではデフォルト有効でした）。

- 実装: `src/metrics/subscriber.ts`（フラグが偽なら即 return）、`src/flag/flag.ts`（`truthy()` による opt-in 化）
- コミット: `b87736db8` (feat: never default to Xiaomi mimo channel and make analytics opt-in)

---

## 5. mimo-free チャネル（api.xiaomimimo.com）の明示オプトイン化 (`MIMOCODE_ENABLE_MIMO_FREE`)

Xiaomi の無料モデル（`mimo-auto`）は 2026-07-26 に終了しましたが、将来 V3.5 / V4.0 の無料モデルが再開される可能性に備えてコードは残しています。ただし中国国内サーバ（`api.xiaomimimo.com`）へのデータ送信を防ぐため、**明示的に `MIMOCODE_ENABLE_MIMO_FREE=true` を設定しない限りこのルートを遮断**します。

- 挙動:
  - `MIMOCODE_ENABLE_MIMO_FREE`（既定 false、getter で動的読み取り）が偽のとき:
    - `bootstrap()` が fetch の前に例外で中断 → ネットワーク接触ゼロ（`src/plugin/mimo-free.ts`）
    - `MimoFreeAuthPlugin` の config フックが空を返し、`mimo` プロバイダ / `mimo-auto` モデルがモデルピッカーに**登録されない**
  - 真のとき: 従来どおりプロバイダ登録 + bootstrap を実行（将来の無料モデル再開時はコード変更なしで再活性化可能）
- Tor 併用時のフィンガープリント回転: `--tor`（`MIMOCODE_TOR`）使用時は、hostname / cpu / ユーザー等から導出するデバイスフィンガープリント（SHA-256）を 1 時間ごとにローテーションします（非永続）。Tor 出口 IP の回転と併せて、同一マシンへのリクエスト再結び付けを防ぎます（非 TOR 時は従来どおり `mimo-free-client` に永続）
- 実装: `src/plugin/mimo-free.ts`（`FINGERPRINT_ROTATE_INTERVAL_MS` / `fingerprint()` / `bootstrap()` / `MimoFreeAuthPlugin`）、`src/flag/flag.ts`（`MIMOCODE_ENABLE_MIMO_FREE` getter）
- テスト: `test/plugin/mimo-free.test.ts`（Tor 時 1 時間回転 / 非 Tor 時安定 / オプトインなしでの verify 拒否・プロバイダ未登録 / オプトインありで登録）
- コミット: 本セクションの追記時点の HEAD

---

## 6. GitHub Releases ベースのインストーラーと配布基盤

npm 配布に依存せず、このリポジトリの GitHub Releases から `oimo-*` アセット（Linux: `.tar.gz` / Windows: `.zip`）を取得・インストールする方式です。

- インストーラー: `install` / `install.ps1` / `install-utf8.ps1` / `local-install.sh`
  - 対応 OS/アーキテクチャの自動判定（linux/darwin/windows × arm64/x64）
  - CPU の **AVX2 非対応を検出して baseline アセット**を選択、`/etc/alpine-release` / `ldd` で **musl (Alpine) を検出**して `-musl` アセットを選択
  - インストール先 `~/.oimo/bin`、シェル設定への PATH 追記、アップグレード通知
- ビルド/リリース: `script/build.ts` の `BINARY_PREFIX = "oimo"`、`script/publish.ts` のフォーク URL・`@mimo-ai/oimo-*` メタデータ、`gh release upload` によるアセット公開
- Xiaomi FDS アップロード（中国大陸向け高速配信）は `MIMO_FDS_AK` / `MIMO_FDS_SK` 設定時のみ実行（省略可）
- コミット: `450b7b25e` (feat: point installers and quick start at this repo's GitHub releases)、`293896e9c`、`526730a67`、`0513986cb`、`03382690c`

---

## 7. 上流への誤 push 防止 pre-push フック

`.husky/pre-push` が、リモート名 `upstream` または URL に `XiaomiMiMo/MiMo-Code` を含む場合に push を拒否します（上流は pull-only の関係）。

- 併せて bun バージョン検証と `bun typecheck`（全 12 タスク）を push 前に実行
- コミット: `736ab8716` (refactor: rename CLI binary from mimo to omimo)

---

## 8. ブランド・リネーム (`oimo` / Open Mimo Code)

- バイナリ名を `mimo` → `omimo` → **`oimo`** に変更、設定ディレクトリを `.oimo`、設定ファイルを `oimo.jsonc` / `oimo.json`、テーマを `oimo.json`、バナーを `oimo-banner.png` にリネーム
- ブランド表記を MiMo Code → **Open Mimo Code** に置換（ソース・i18n・README 3 言語・スキル等）
- 独自テーマ「bright purple」を既定のブランドカラーとして設定
- 注: `mimo.xiaomi.com/mimocode/*` スキーマ URL と `mimocode-docs` スキル名は互換性のため維持
- コミット: `736ab8716`、`2c45b368b` (feat(theme): use bright purple as omimo brand color)、`fb527270c`、`ace81a693` (docs: use logo as the README logo; 画像は後に `assets/readme/oimo.png` へリネーム)

---

## 9. TUI セッションログ（`oimo --log <ファイル>`）と質問ダイアログの確定操作

### --log（質問と処理後のサマリーを 1 ファイルに追記）

TUI モードで、ユーザーの質問と処理後のサマリー（最終応答）を Markdown ファイルに随時追記します。

- 起動フラグ: `oimo --log <file.md>`（グローバルオプション。TUI モードで有効）、環境変数 `MIMOCODE_LOG`
- 挙動:
  - assistant メッセージが完了（`time.completed`）するたびに、直前のユーザー質問とサマリー（text パーツ）を `## 日時` / `### User` / `### oimo` / `---` 形式でファイル末尾へ追記
  - 質問で終わるターン（Plan モードの決定確認など）もそのまま記録
  - 同一メッセージの二重書き込みは assistant messageID で防止、追記失敗は warn のみで TUI を停止しない
- 実装: `src/cli/cmd/tui/session-log.ts`、`src/cli/cmd/tui/context/sync.tsx`、`src/index.ts`（グローバル `--log` とミドルウェア）、`src/flag/flag.ts`（`MIMOCODE_LOG`）
- テスト: `test/cli/tui/session-log.test.ts`
- コミット: `57130cf01` (feat(tui): add --log to append each question and summary to a markdown file)

### 質問ダイアログの Confirm クリック確定

- 原因: 質問ダイアログ（Plan モードの `plan_exit` 決定確認など）で、Confirm タブは confirm 状態でも `selectTab` の no-op になり、マウス操作だけでは確定できなかった
- 修正: confirm 状態で Confirm をクリックすると `submit()` するよう変更（`src/cli/cmd/tui/routes/session/question.tsx`）
- コミット: `57130cf01`（上記と同じ）

---

## 10. TUI 周りの UX 改善（`-c` のセッションなし時のフォールバック / `session list` の対話選択）

### `-c`（--continue）で継続可能セッションが無いときの新規セッション開始

- 原因: `-c` 起動時は初期ルートを `session/"dummy"`（実在しないセッション）にしてから最新ルートセッションを探すため、セッションが 1 件も無いと `match` が無く「dummy セッション」のまま無応答になる
- 修正: 最新ルートセッションが見つからない場合は `home`（新規セッション画面）へ遷移する（`src/cli/cmd/tui/app.tsx` の `-c` 再開用 effect）
- コミット: 本セクションの追記時点の HEAD

### `oimo session list` の対話選択（矢印キー + Enter でセッションを選択して TUI 起動）

- 挙動: TTY では一覧を表示する代わりに clack の `select` で「＋ New session」と各セッションを表示。矢印キーで選んで Enter で、そのセッションを続行する TUI（`oimo --session <id>`）を子プロセスとして起動。ESC/Ctrl-C で何もせず終了。非 TTY や `--format json` は従来どおり表/JSON を出力
- 実装: `src/cli/cmd/session.ts`（`pickAndLaunch` / `launchTui`。`less` によるページャー表示は廃止）
- 起動コマンド: リリースバイナリでは `process.execPath --session <id>`、開発時は `process.execPath <entry> --session <id>`
- コミット: 本セクションの追記時点の HEAD

---

## 11. OpenCode Zen（Big Pickle 等）向け公式互換ヘッダー

OpenCode Zen の無料枠（Big Pickle など）は、公式クライアント識別ヘッダーが無いと `dailyRequestsFallback` に落ち、すぐレート制限される。oimo はブランド UA が `oimo/...` で `x-opencode-*` も送っていなかった。

- Zen（`providerID` が `opencode` で始まる）への LLM 呼び出しだけ、公式 OpenCode と同じヘッダーを付ける:
  - `User-Agent: opencode/<version>`
  - `x-opencode-client`（`MIMOCODE_CLIENT`、既定 `cli`）
  - `x-opencode-session` / `x-opencode-request` / `x-opencode-project`
- その他プロバイダは従来どおり `oimo/<version>` + `x-session-affinity`
- 実装: `src/session/provider-headers.ts`、呼び出し元は `src/session/llm.ts` / `src/session/prompt.ts` / `src/mcp/sampling.ts`
- テスト: `test/session/provider-headers.test.ts`

---

## 12. マルチリポジトリ（System Workspace）

単一 cwd 前提のコーディングエージェントから、**複数独立 Git リポジトリを一つのシステムとして扱う**基盤。業務機能単位の依頼に対し、関連リポを登録・識別し、境界を守ったまま横断作業へ進める。

**設定キーの全文:** [`docs/multi-repo/config-reference.ja.md`](docs/multi-repo/config-reference.ja.md)

### 発見順

1. `.oimo/workspace.yaml`（`.yml` / `.json` / `.jsonc` も可）
2. `.oimo/repos.txt`（`repos.list` / `repositories.txt` も可）
3. `.gitmodules` → superproject モード

### `repos.txt`（推奨）

```text
# <workspace>/.oimo/repos.txt
name: customer-platform
primary: backend
https://github.com/org/frontend
https://github.com/org/backend
https://github.com/org/shared-schema
https://github.com/org/infra read-only
```

| 行 / トークン | 意味 |
|---------------|------|
| `name: …` | Workspace 表示名（省略時 `workspace`） |
| `primary: …` | 主対象の **id**（省略時は先頭リポ行） |
| `https://…` / `git@…` | リモート。ローカルは `<workspace>/<id>/` を期待 |
| ローカルパス | 既存 clone（相対は Workspace ルート基準） |
| `read-only` | 書込禁止 |
| `id=<id>` | Registry id（省略時は URL/パス末尾） |
| `role=<文言>` | 役割メモ（空白なしの短い文字列） |

手順: ファイルを置く → 兄弟へ `git clone`（`docs/multi-repo/samples/url-list/`）→ oimo 起動。

### `workspace.yaml`（詳細）

| キー | 意味 |
|------|------|
| `version` | `1` 固定 |
| `name` / `primary` | 表示名 / 主対象 id（`repositories[].id` のいずれか） |
| `repositories[].id` | 安定 id（表示は `id:相対パス`） |
| `repositories[].path` | 絶対または相対。`kind: git` なら **worktree ルート必須** |
| `repositories[].role` | 役割説明 |
| `repositories[].access` | `read-only` \| `read-write` |
| `repositories[].kind` | `git`（既定）/ `directory`（非 Git・明示必須）/ `superproject` / `submodule` |
| `defaults.allow_unregistered_reads` | 既定 `false` |
| `defaults.allow_unregistered_writes` | 既定 `false`（`true` は非推奨） |
| `defaults.require_cross_repo_plan` | 既定 `true` |

サンプル: `docs/multi-repo/samples/siblings-yaml/workspace.yaml`

### `.gitmodules`（superproject）

| Git キー | oimo での意味 |
|----------|---------------|
| `[submodule "name"]` | 論理名 → id 候補 |
| `path` | superproject からの相対 checkout 位置（必須） |
| `url` | clone / 追跡リモート |
| `branch` | 追跡ブランチ（behind 警告用。**fetch しない**） |

自動登録: 親 = `kind: superproject`（原則 read-only）+ 各子 = `kind: submodule`。  
検査: recorded/checked-out commit、初期化・dirty・不一致・behind → 警告。`submodule update` は自動実行しない。実装・commit・PR は submodule 側、ポインタ更新は別 Change set。

サンプル: `docs/multi-repo/samples/superproject/gitmodules.sample`

### 安全境界（Resolver）

- 未登録パスへの書込拒否、`read-only` 強制、`repo-id:relpath` 表現
- セッション指紋（config・repo 一覧・submodule 状態）を保存し、移動推測はしない

### 実装・文書

- 実装: `packages/opencode/src/repo-workspace/`
- 利用ガイド: `docs/multi-repo/README.md`（日本語） / `docs/multi-repo/README.en.md`
- **設定リファレンス:** `docs/multi-repo/config-reference.ja.md`
- コピペ用サンプル: `docs/multi-repo/samples/`（[samples/README.md](docs/multi-repo/samples/README.md)）
- 設計: `docs/multi-repo/current-architecture.md` / `implementation-plan.md`
- テスト: `packages/opencode/test/repo-workspace/`

> Phase 1 基盤（登録・Resolver・doctor・指紋・superproject）。横断検索・影響グラフ・計画付き横断編集は後続 Phase。

---

## 付録: opencode からの移植（オリジナル実装ではない）

- `oimo run --auto`（`--yolo` / `--dangerously-skip-permissions` のエイリアス）: upstream opencode の `run --auto` と同じ動作を移植したもの。opencode に存在するため上記の「オリジナル実装」には含めていません。TUI モード（`oimo --auto`）にも `--dangerously-skip-permissions` と同等のエイリアスとして追加済み。
  - コミット: `8954ae52d` (feat(run): add --auto to auto-approve permissions in oimo run)
- 外部送信監査レポート: `外部送信監査.md` はドキュメント（実装ではない）ですが、本リポジトリ独自の公開資料です。
  - コミット: `a64e942de` (docs: add external data transmission audit report)
